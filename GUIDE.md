# 서비스 실행 가이드

처음부터 끝까지 실제로 돌리는 절차를 순서대로 정리한 문서다.
설계 배경과 구조는 [README.md](README.md)를 참고한다.

**크롤링을 포함한 모든 작업은 웹 UI에서 버튼으로 실행할 수 있다.** 콘솔 명령은
같은 동작의 대체 수단이며, 자동화하거나 서버를 띄우지 않고 돌릴 때 쓴다.

```bash
cd "/Users/msk/Documents/Claude/Projects/PF_Website_Classification-And-Review-System_single"
./venv/bin/python run.py web        # → http://127.0.0.1:8000
```

콘솔 명령은 모두 **프로젝트 루트**에서 실행한다.

---

## 0. 한눈에 보는 전체 흐름

| 단계 | 웹 UI (진행 상황 화면) | 콘솔 | 소요 |
|---|---|---|---|
| [0] 준비 | — | `run.py init` | 1분 |
| [1] 크롤링 ★ | **크롤링 시작** / 실패분 재시도 / 중지 | `run.py fetch` / `retry` | 4~8시간 (분할) |
| [2] 한국어 판정 | 한국어 판정 / 전량 재계산 | `run.py detect-korean` | 수 분 |
| [3] 규칙 분류 | 규칙 분류 / 전량 재분류 | `run.py classify-rule` | 수 분 |
| [4] 튜닝 루프 | 카테고리 규칙 · 사이트 검수 화면 | — | (반복) |
| [5] 세션 판정 | **자동 판정 실행** (또는 수동: 내보내기→세션→반영) | `run.py llm-auto` | (반복) |
| [6] 산출물 | 산출물 생성 | `run.py export` | 수십 초 |

**[1]만 오래 걸리고 나머지는 전부 빠르다.** [2]~[3]은 네트워크를 쓰지 않아 몇 번이든
다시 돌릴 수 있으므로, 기준을 바꿔가며 반복하는 것을 전제로 만들었다.

언제든 중단해도 된다. 다시 시작하면 처리되지 않은 것부터 이어서 진행한다.

> **작업은 한 번에 하나만 돈다.** 같은 테이블을 쓰기 때문에 겹치면 서로 방해한다.
> 실행 중에 다른 작업을 시작하려 하면 거절되고, 무엇이 돌고 있는지 알려준다.

---

## 1. 준비 (최초 1회)

```bash
python3 -m venv venv
./venv/bin/pip install -r requirements.txt
./venv/bin/python run.py init
```

`init` 은 다음을 수행한다. 이미 되어 있으면 건너뛰므로 여러 번 실행해도 안전하다.

- `data/sites.db` 생성
- `top-1m.csv` 100만 행 적재
- 기본 설정 20개, 카테고리 규칙 284개 주입 (웹에서 고친 값은 덮어쓰지 않음)

확인:

```bash
./venv/bin/python run.py status
```

---

## 2. 크롤링

가장 오래 걸리는 단계다. 지정한 건수만 처리하고 끝나므로, 나눠서 여러 번 실행한다.

**웹 UI** — 진행 상황 화면의 크롤링 카드에서 **건수**와 **동시 요청 수**를 넣고
`크롤링 시작`. 진행률이 3초마다 갱신되고, `중지` 버튼으로 언제든 세울 수 있다
(진행 중인 요청을 마무리하느라 십여 초 걸린다). 건수를 `0` 으로 두면 전체가 대상이다.

**콘솔**

```bash
./venv/bin/python run.py fetch --limit 50000
```

이걸 20번 반복하면 100만 건이 끝난다. 한 번에 전부 돌리려면 `--limit 0`.

| 옵션 | 설명 |
|---|---|
| `--limit N` | 이번 실행에서 처리할 건수 (0 = 전체, 기본 50000) |
| `--concurrency N` | 동시 요청 수 (기본: 설정값 150) |

**진행 표시**

```
대상 50,000건, 동시 150개로 시작합니다. Ctrl+C로 언제든 중단 가능합니다.
  12,400/50,000  성공 8,102  실패 4,298  78.3건/초  남은 예상 8분
```

**중단하려면** 웹의 `중지` 버튼, 콘솔이라면 Ctrl+C 를 한 번 누른다. 어느 쪽이든
진행 중이던 요청만 마무리하고 저장한 뒤 멈춘다. (콘솔에서 Ctrl+C 를 한 번 더 누르면
강제 종료되며, 마지막 커밋 이후 200건 이내가 유실된다.)

**재개하려면** 다시 시작한다. 이미 처리한 도메인은 자동으로 건너뛴다.

### 오래 돌릴 때

**웹으로 돌리는 경우** 크롤링은 웹 서버 프로세스 안에서 돈다. 따라서
**서버를 끄면 크롤링도 멈춘다** (저장된 데이터는 그대로이고 다시 시작하면 이어진다).
브라우저 탭을 닫는 것은 상관없다 — 서버만 살아 있으면 계속 진행된다.
터미널을 닫아도 서버를 유지하려면:

```bash
nohup ./venv/bin/python run.py web > web.log 2>&1 &
```

**콘솔로 돌리는 경우**

```bash
nohup ./venv/bin/python run.py fetch --limit 0 > fetch.log 2>&1 &
tail -f fetch.log          # 진행 상황 보기
```

어느 쪽이든 돌아가는 동안 다른 터미널에서 `run.py status` 로, 또는 웹 UI로 진행률을
볼 수 있다. (SQLite를 WAL 모드로 쓰기 때문에 크롤링 중에도 조회가 막히지 않는다.)

콘솔에서 돌린 크롤링도 웹 진행 상황 화면에 **`크롤링 (콘솔)`** 로 표시되고 진행률이
실시간으로 갱신된다. 다만 다른 프로세스라 웹의 `중지` 버튼으로는 세울 수 없다 —
터미널에서 Ctrl+C 를 누르거나 `pkill -INT -f "run.py fetch"` 를 쓴다.
콘솔 작업이 도는 동안에는 웹의 실행 버튼이 잠긴다(중복 실행 방지).

### 실패분 재시도

웹의 `실패분 재시도` 버튼, 또는

```bash
./venv/bin/python run.py retry
```

- `failed` (타임아웃, 5xx, SSL 오류 등 일시적 실패)만 대상으로 한다
- `dead` (DNS 없음 등 회복 불가)는 재시도하지 않는다
- 도메인당 최대 3회까지 (설정 `fetch.max_attempts`)

### 실패가 많아 보인다면

상위 300건 파일럿 기준 **접속 불가 25%** 가 나왔는데 정상이다. tranco 상위에는
`gtld-servers.net`, `akamai.net`, `fbcdn.net` 처럼 웹서버가 없는 인프라/CDN 도메인이
많이 섞여 있다. 원인별 분포는 이렇게 확인한다.

```bash
./venv/bin/python -c "
from src import db
c = db.connect()
for r in c.execute('''SELECT substr(error,1,50) e, COUNT(*) n FROM sites
    WHERE fetch_status IN (\"failed\",\"dead\") GROUP BY e ORDER BY n DESC LIMIT 10'''):
	print('%6d  %s' % (r['n'], r['e']))
"
```

---

## 3. 한국어 판정

```bash
./venv/bin/python run.py detect-korean
```

크롤링이 끝난 사이트만 대상으로 한다. 네트워크를 쓰지 않아 수 분이면 끝난다.

기준을 바꾼 뒤 전부 다시 계산하려면:

```bash
./venv/bin/python run.py detect-korean --force
```

### 판정 기준

신호별 점수를 더해 **임계값(기본 100) 이상**이면 한국어로 본다.

| 신호 | 기본 점수 |
|---|---|
| `.kr` / `.한국` TLD | 100 (이것만으로 확정) |
| `html lang=ko` 또는 `og:locale=ko_KR` | 80 |
| 본문 한글 비율 ≥ 10% | 80 |
| charset EUC-KR / CP949 | 60 |
| title·description에 한글 포함 | 40 |
| 본문 한글 비율 3~10% | 30 |

### 글로벌 사이트가 한국어로 잡히는 이유

크롤링할 때 `Accept-Language: ko-KR` 헤더를 보내기 때문에, 다국어 사이트는 한국어
페이지를 내주고 그대로 한국어로 판정된다. **의도한 동작이다** — naver.com뿐 아니라
"한국어 서비스를 제공하는" google.com, netflix.com도 대상에 넣기 위한 설계다.

다만 `doubleclick.net`, `googlevideo.com` 같은 광고/CDN 도메인도 함께 들어온다.
판정 근거가 `korean.reasons` 에 남으므로 나중에 분리할 수 있다.

```bash
# TLD만으로 잡힌 것 / 실제 한국어 콘텐츠 구분해서 보기
./venv/bin/python -c "
from src import db
c = db.connect()
for r in c.execute('''SELECT reasons, COUNT(*) n FROM korean WHERE is_korean=1
    GROUP BY reasons ORDER BY n DESC LIMIT 15'''):
	print('%7d  %s' % (r['n'], r['reasons']))
"
```

노이즈가 많다고 판단되면 웹 UI의 **판정 기준** 화면에서 임계값을 올리거나
개별 신호 점수를 낮춘 뒤 전량 재판정한다.

### 국내에서 차단된 사이트

불법·유해 사이트에 접속하면 ISP가 방송통신심의위원회 안내 페이지(`warning.or.kr`)로
돌려보낸다. 그러면 **차단 안내문이 수집되어** 실제 사이트와 무관한 한국어 페이지가
저장되고, 그대로 두면 성인·불법 사이트가 "한국어 서비스"로 판정되어 결과에 섞인다.

크롤링할 때 도착 URL을 보고 자동으로 `fetch_status = 'blocked'` 로 표시한다.
이미 수집한 데이터를 정리하려면:

```bash
./venv/bin/python run.py mark-blocked
```

- 차단 사이트는 한국어 판정·분류·산출물에서 모두 빠진다
- 목록은 남는다 — 사이트 검수 화면의 `차단여부` 필터에서 `국내 차단만` 으로 볼 수 있다
- 사람이 지정한 검수 상태·수동 라벨·LLM 판정은 건드리지 않는다
- 여러 번 실행해도 안전하다

### 용량 정리 (선택)

비한국어 사이트의 본문 발췌를 비우고 DB를 압축한다. 크롤링을 상당히 진행한 뒤 한 번씩.

```bash
./venv/bin/python run.py compact
```

---

## 4. 규칙 분류

```bash
./venv/bin/python run.py classify-rule
```

한국어로 판정된 사이트를 대상으로 카테고리 점수를 매긴다. 역시 네트워크 불필요.
규칙을 고친 뒤 전부 다시 계산하려면 `--force`.

결과는 세 가지로 나뉜다.

| 상태 | 의미 | 다음 처리 |
|---|---|---|
| `rule` (확신도 높음) | 규칙으로 확정 | 그대로 사용 |
| `rule` (확신도 낮음) | 1·2위가 붙어 있어 애매 | 세션 판정으로 넘어감 |
| `unclassified` | 매칭된 규칙 없음 | 세션 판정으로 넘어감 |

확신도 기준값은 설정 `classify.llm_conf_threshold` (기본 0.6).

---

## 5. 튜닝 루프 — 웹 UI

### 서버 켜기

```bash
./venv/bin/python run.py web
```

브라우저에서 <http://127.0.0.1:8000> 을 연다. 로컬 전용이라 인증이 없다.
포트를 바꾸려면 `--port 9000` 처럼 붙인다.

터미널을 닫아도 계속 띄워두려면:

```bash
nohup ./venv/bin/python run.py web > web.log 2>&1 &
```

### 서버 끄기

**터미널에서 직접 띄웠다면** 그 터미널에서 `Ctrl+C` 를 누른다.

**`nohup` 으로 띄웠거나 터미널을 잃어버렸다면** 아래 중 하나를 쓴다.

```bash
pkill -f "run.py web"
```

포트로 찾아서 끄는 방법 (다른 파이썬 프로세스를 건드리지 않아 더 안전하다):

```bash
kill $(lsof -ti:8000)
```

끄기 전에 확인하고 싶다면:

```bash
pgrep -fl "run.py web"      # 실행 중인 서버와 PID 보기
lsof -ti:8000               # 8000 포트를 쓰는 PID 보기
```

종료됐는지 확인:

```bash
pgrep -f "run.py web" || echo "종료됨"
```

> **크롤링 중이라면 먼저 중지하라.** 크롤링은 웹 서버 프로세스 안에서 돌기 때문에
> 서버를 끄면 함께 멈춘다. 진행 상황 화면의 `중지` 버튼을 눌러 저장이 끝난 것을
> 확인한 뒤 서버를 끄면 마지막 커밋 이후 분량(최대 200건)을 다시 처리하지 않아도 된다.
> 그냥 꺼도 데이터가 깨지지는 않는다 — 다시 시작하면 이어서 진행한다.

`nohup` 으로 돌린 **크롤링**을 끄는 것도 같은 방식이되, 신호를 `-INT` 로 준다.

```bash
pkill -INT -f "run.py fetch"     # 진행 중인 요청을 마무리하고 저장한 뒤 종료
```

`pkill -f` (신호 없이)는 SIGTERM이라 저장하지 않고 즉시 죽는다. 크롤링은 `-INT` 를
붙여야 안전하게 끝난다.

**바로 죽지 않는 것이 정상이다.** 진행 중이던 요청이 타임아웃될 때까지 기다렸다가
저장하고 끝나므로 **10~20초쯤 걸린다.** 그 사이 로그에 `중단 요청 —` 이 찍히고,
끝나면 `중단됨: N건 처리` 가 나온다. 안 죽는 것처럼 보여도 `kill -9` 는 쓰지 않는다.
(강제로 죽이면 마지막 커밋 이후 최대 200건을 다시 크롤링하게 될 뿐, 데이터가
깨지지는 않는다.)

웹 UI의 `중지` 버튼도 마찬가지로 십여 초 뒤에 멈춘다.

### 화면별 용도

**진행 상황** — 크롤링·판정·분류 진행률, 카테고리별 건수, 배치 현황을 3초마다 갱신.
**모든 작업을 여기서 실행한다.**

| 카드 | 버튼 |
|---|---|
| 크롤링 | 크롤링 시작 · 실패분 재시도 · 중지 (건수·동시 요청 수 지정) |
| 한국어 판정 | 한국어 판정 · 전량 재계산 · 본문 정리 + 압축 |
| 분류 | 규칙 분류 · 전량 재분류 |
| 세션 판정 배치 | 자동 판정 실행 · 배치 내보내기 · 배치별 반영 |
| 산출물 | 산출물 생성 |

실행 중에는 상단에 진행 상황 배너가 뜨고 다른 버튼이 잠긴다. 끝나면 결과가 알림으로
표시되며, 실패하면 배너에 오류 내용이 남는다. 중지할 수 있는 작업은 크롤링뿐이고
나머지는 몇 분 안에 끝난다.

**카테고리 규칙** — 이 화면이 튜닝의 중심이다.

1. 카테고리를 고른다
2. 규칙을 추가하기 전에 **미리보기**를 눌러 몇 건이 매칭되는지, 샘플이 어떤지 확인한다
3. 확인되면 **추가**
4. 다 고쳤으면 **규칙 반영해 전체 재분류**

규칙 종류는 세 가지다.

| 종류 | 매칭 대상 | 규칙 |
|---|---|---|
| 도메인 패턴 | 도메인 | 기본은 **토큰 정확 일치**. `ai` 는 `mail` 에 걸리지 않는다.<br>부분일치가 필요하면 `sec.co.kr` 처럼 `.` 을 넣거나 `*sec*` 와일드카드 |
| 키워드 | title + description + 본문 발췌 | 부분일치 (대소문자 무시) |
| 제외어 | 위와 동일 | 걸리면 그 카테고리 점수를 0으로 만든다 |

가중치 관례: 서비스 확정 도메인 3.0 / 일반 토큰 1.5 / 키워드 1.0

**판정 기준** — 한국어 판정 가중치·임계값, 크롤러 설정, 배치 크기를 폼으로 수정한다.
저장 후 **전량 재판정** 버튼을 눌러야 반영된다. 크롤러 설정은 다음 `fetch` 실행부터 적용.

**사이트 검수** — 결과를 눈으로 확인하고 최종 상태를 정하는 화면.

- 도메인/제목 검색, 한국어 여부, 카테고리, 분류 방식, **검수 상태**, 순위 구간으로 필터
- 각 행에 판정 근거(`한국어: lang_ko,hangul_37% (200점) · 분류: SNS 3.0, 뉴스 2.0`)가 보인다
- **수정** 버튼으로 **제목**, 최종 상태, 카테고리, 한국어 여부를 한 화면에서 지정한다.
  이렇게 고친 값은 재크롤링·재분류해도 덮어쓰이지 않는다

### 접속하면 다른 주소로 가는 사이트

도메인 아래에 실제 도착 주소가 `↳` 로 표시된다. 다른 도메인으로 가면 **빨간색**,
서브도메인 정도면 회색이다 (`www` 차이는 같은 것으로 본다).
도메인을 클릭하면 리다이렉트를 한 번 더 타지 않고 실제 페이지로 바로 간다.

실제로 절반 가까이가 다른 주소로 이동한다. `googlevideo.com` 이 `google.com` 으로
가서 제목이 "Google"로 붙는 식이라, 이걸 모르면 엉뚱한 페이지를 보고 판단하게 된다.
`주소이동` 필터로 이동하는 것만 몰아서 검수할 수 있다.

**도착 주소를 목록에 추가하기** — 도착 주소가 아직 목록에 없으면 `+ 추가` 버튼이 뜬다.
누르면 **원본과 같은 순위로 추가하고, 그 자리에서 수집·한국어 판정·규칙 분류까지** 끝낸다
(몇 초 걸린다). 이미 목록에 있으면 `·목록에 있음` 으로 표시되고 버튼은 나오지 않는다.

`amazonaws.com`(순위 9)의 도착지인 `aws.amazon.com` 을 추가하면 순위 9로 들어가고,
바로 제목·판정·분류가 채워진 채 목록에 나타난다.

### 제목 수정

크롤링으로 가져온 제목이 비어 있거나(`(제목 없음)`), 광고 문구뿐이거나, 사이트 이름을
알아보기 어려울 때 직접 고친다. 수정한 제목은 목록에 `수정됨` 표시가 붙고,
검색과 산출물에도 그대로 반영된다.

- 원래 제목은 입력란 아래에 `수집한 제목: ...` 으로 남아 있고,
  **수집한 제목으로 되돌리기** 버튼으로 언제든 되돌릴 수 있다
- 수집한 제목과 똑같이 입력하면 수정본으로 저장하지 않는다
- **수동 수정 해제** 를 누르면 제목·카테고리·한국어 여부를 한꺼번에 원상복구한다

### 특정 사이트를 LLM에 분석 요청하기

규칙 분류가 미덥지 않거나 판단이 어려운 사이트는 카테고리 칸의 **`LLM 요청`** 버튼으로
판정 대기열에 넣는다. 목록 상단의 `LLM 분석 요청` 으로 현재 페이지를 한꺼번에 넣을 수도 있다.

요청하면 **다음 배치에 우선 실려 나간다.** 이후 절차는 평소와 같다.

```
LLM 요청 (검수 화면)
   ↓
진행 상황 화면의 `배치 내보내기`   ← 요청한 건이 맨 앞에 들어간다 (배치 파일에 ★ 표시)
   ↓
Claude Code 세션에서 분류
   ↓
`반영` 버튼
```

- 요청한 사이트는 `LLM 요청됨` 으로 표시되고, 배치로 나가기 전까지는 `취소` 할 수 있다
- 배치로 나간 뒤에는 `LLM 분석 중` 으로 바뀌고 취소할 수 없다
- **수동으로 지정한 사이트**도 요청할 수 있지만, 사람 라벨이 우선이라 LLM 결과가
  카테고리를 덮어쓰지 않는다 (요청할 때 안내가 뜬다)
- 한국어로 판정되지 않은 사이트도 직접 요청하면 배치에 실린다.
  자동 선정은 한국어 사이트 중에서만 이뤄진다

### 최종 상태 (검수완료 / 제외)

각 행의 **검수** · **제외** · **해제** 버튼으로 한 번에 지정한다. 목록 상단의
`이 페이지 전체를` 버튼으로 현재 페이지 50건을 한꺼번에 처리할 수도 있고,
**수정** 다이얼로그에서는 카드 세 장 중 하나를 눌러 고른다 (제외를 고르면 사유 입력란이
나타난다).

| 상태 | 의미 | 산출물 |
|---|---|---|
| 미검수 | 아직 확인하지 않음 (기본값) | 포함 |
| 검수완료 | 사람이 확인함 | 포함 |
| 제외 | 최종 결과에서 뺀다 | **빠짐** — `excluded_sites.csv` 에만 남음 |

제외는 광고·CDN 도메인처럼 한국어로는 잡혔지만 결과에 넣고 싶지 않은 사이트를
걸러내는 용도다. 제외 사유를 적어두면 `excluded_sites.csv` 에 함께 나온다.

이 상태는 분류 라벨과 독립적이라, 재분류·재판정을 몇 번 돌려도 유지된다.
검수 진행 상황은 진행 상황 화면과 `run.py status` 에서 확인할 수 있다.

**권장 사용법** — 검수 상태 필터를 `미검수` 로 두고 위에서부터 훑으면서 처리하면,
이미 본 것이 목록에서 빠지므로 어디까지 봤는지 따로 기억할 필요가 없다.

### 권장 튜닝 순서

1. 사이트 검수에서 카테고리 하나를 골라 목록을 훑는다
2. 잘못 들어온 사이트가 있으면 → 카테고리 규칙에서 제외어를 추가하거나 가중치를 낮춘다
3. 빠진 사이트가 보이면 → 미리보기로 확인한 뒤 도메인/키워드 규칙을 추가한다
4. **규칙 반영해 전체 재분류** → 1번으로 돌아간다

분류는 항상 전체 규칙으로 계산한다. 한 카테고리만 다시 돌리면 나머지 카테고리의
기존 결과가 지워지기 때문이다.

---

## 6. 세션 판정 (LLM)

규칙으로 확정되지 않은 사이트를, 콘솔에 열어둔 Claude Code 세션에 넘겨 판정한다.
API 키도 과금도 없다. **파일을 사이에 두고 주고받는 방식**이다.

### 자동으로 돌리기 (권장)

`claude` CLI를 불러 **배치 생성 → 판정 → 반영을 알아서 반복**한다.
콘솔에 세션을 열어두고 붙여넣을 필요가 없다.

```bash
./venv/bin/python run.py llm-auto --check          # CLI를 쓸 수 있는지 먼저 확인
./venv/bin/python run.py llm-auto --rounds 5       # 배치 5개 자동 처리
./venv/bin/python run.py llm-auto --rounds 0       # 대상이 없어질 때까지
```

진행 상황 화면의 **세션 판정 배치** 카드에서 `자동 판정 실행` 버튼으로도 돌릴 수 있다.
배치 수를 `0` 으로 두면 대상이 떨어질 때까지 계속 돈다. 실행 중에는 `중지` 로 세울 수 있다.

**전제 조건** — 터미널에서 `claude` 가 로그인되어 있어야 한다. 확인:

```bash
claude -p "안녕"
```

`Failed to authenticate` 가 나오면 터미널에서 `claude` 를 한 번 실행해 로그인한다.
자동 판정은 그 로그인 세션을 그대로 쓴다 (API 키·별도 과금 없음).

**실패하면 되돌린다** — 판정이 실패하면 그 배치를 취소하고 대상을 큐로 돌려놓는다.
그대로 두면 거기 실린 도메인이 "이미 나간 건"으로 묶여 다음 배치에서도 빠지기 때문이다.
사람이 직접 요청했던 건은 요청 상태로 복구된다.

### 손으로 한 사이클 돌리기

**웹 UI** — `배치 내보내기` → 화면에 뜬 문장을 Claude Code 세션에 붙여넣기 →
세션이 결과 파일을 만들면 배치 목록의 `반영` 버튼. 콘솔을 열 필요는 세션에 지시할
때뿐이다.

**콘솔**

```bash
# ① 배치 파일 생성
./venv/bin/python run.py llm-export --size 40
```

실행하면 붙여넣을 문장까지 함께 출력된다.

```
배치 batch_0001 생성: 40건 → llm/requests/batch_0001.md

──────────────────────────────────────────────────────────────
아래 문장을 콘솔의 Claude Code 세션에 붙여넣으세요:

  llm/requests/batch_0001.md 를 읽고 지시대로 분류해서 llm/results/batch_0001.json 로 저장해줘

완료되면:  python run.py llm-import llm/results/batch_0001.json
──────────────────────────────────────────────────────────────
```

② 그 문장을 Claude Code 세션에 붙여넣는다. 세션이 파일을 읽고 분류해서 결과를 저장한다.

```bash
# ③ 결과 반영
./venv/bin/python run.py llm-import llm/results/batch_0001.json
```

④ ①로 돌아간다. `llm-export` 는 아직 판정되지 않은 도메인을 자동으로 집어온다.

### 알아둘 점

- **배치 파일은 자기완결적이다.** 카테고리 정의와 출력 스키마가 매번 파일 머리에
  들어가므로, 어느 세션에서 열어도 앞선 대화 맥락 없이 그대로 처리된다.
- **컨텍스트가 쌓이지 않는다.** 배치 하나를 끝내면 그 내용은 잊어도 되므로,
  100배치를 연속으로 처리해도 세션이 무거워지지 않는다.
- **중복되지 않는다.** 이미 판정된 도메인은 다음 배치에 다시 실리지 않는다.
- **빠진 건 자동 복구된다.** 결과 JSON에서 누락된 도메인은 큐에 남아 다음 배치로 넘어간다.
- **같은 파일을 두 번 import 해도 안전하다.**
- 배치 크기 기본 40건. `--size` 로 조절하거나 설정 `llm.batch_size` 를 바꾼다.

### 남은 배치 확인

```bash
./venv/bin/python run.py llm-status
```

```
반영 완료 배치: 12개 / 대기 중인 도메인: 40건
  batch_0013 (exported, 40건) 결과파일 있음     ← import 하면 된다
```

웹 UI의 진행 상황 화면에서도 같은 정보를 볼 수 있고, **배치 내보내기** 버튼으로
①을 대신할 수 있다.

---

## 7. 산출물

두 가지를 만들 수 있다.

| | 웹 버튼 | 콘솔 | 출력 위치 | 담기는 것 |
|---|---|---|---|---|
| 전체 | `전체 산출물 생성` | `run.py export` | `out/` | 미검수 + 검수완료 (제외분만 뺌) |
| 검수완료분 | `검수완료분만 생성` | `run.py export --reviewed-only` | `out/reviewed/` | **검수완료로 표시한 것만** |

두 결과는 서로 덮어쓰지 않으므로 나란히 두고 비교할 수 있다.
검수를 진행하면서 **지금까지 확정된 것만** 뽑아 쓸 때 `검수완료분만` 을 쓴다.

각 폴더에 아래 파일이 생긴다.

| 파일 | 내용 |
|---|---|
| `korean_sites.csv` | 한국어로 판정된 사이트 (순위, 도메인, 제목, 점수, 판정 근거, 검수 상태) |
| `classified_sites.csv` | 분류 결과 전체 (대표 카테고리, 전체 카테고리, 분류 방식, 확신도, 근거) |
| `by_category/{카테고리}.csv` | 카테고리별 파일 10개 |
| `excluded_sites.csv` | 검수에서 **제외**로 표시한 사이트와 그 사유 (전체 산출물에만) |
| `summary.md` | 크롤링·판정·분류·검수 집계 요약 |

검수에서 제외한 사이트는 `excluded_sites.csv` 를 뺀 나머지 파일에서 모두 빠진다.
`out/reviewed/summary.md` 에는 검수완료분만 담겼다는 안내가 함께 들어간다.

CSV는 `utf-8-sig` 로 저장하므로 엑셀에서 바로 열어도 한글이 깨지지 않는다.
멀티라벨 사이트는 해당하는 모든 카테고리 파일에 들어간다.

---

## 8. 자주 겪는 상황

**크롤링이 너무 느리다**
`--concurrency` 를 올린다. 다만 150을 넘기면 로컬 네트워크나 DNS가 병목이 되어
오히려 타임아웃이 늘 수 있다. 파일럿에서 동시 50일 때 25~36건/초가 나왔다.

**중간에 강제 종료됐다**
그냥 다시 시작하면 된다. 마지막 커밋 이후 200건 이내만 다시 처리된다.
`run.py status` 의 "최근 작업" 이 `running` 으로 남아 있어도 `run.py web` 을 한 번
실행하면 정리된다.

**웹에서 크롤링을 돌리다 서버를 껐다**
크롤링도 함께 멈춘다 (웹 서버 프로세스 안에서 돌기 때문). 저장된 데이터는 그대로이니
서버를 다시 띄우고 `크롤링 시작` 을 누르면 이어서 진행한다. 서버를 계속 띄워두기
어렵다면 크롤링만 콘솔에서 `nohup ... run.py fetch` 로 돌리는 편이 낫다.

**"○○ 작업이 실행 중입니다" 라며 거절된다**
작업은 한 번에 하나만 돈다. 진행 상황 화면 상단 배너에서 무엇이 돌고 있는지 확인하고,
끝난 뒤에 다시 누른다. 크롤링이라면 `중지` 로 세울 수 있다.

**중지를 눌렀는데 안 멈춘다 / 서버가 안 꺼진다**
정상이다. 진행 중이던 요청이 타임아웃될 때까지 기다렸다가 저장하므로 10~20초쯤 걸린다.
그동안 기다린다. `kill -9` 로 강제 종료하면 마지막 커밋 이후 최대 200건을 다시
크롤링하게 된다 (데이터가 깨지지는 않는다).

**포트가 이미 사용 중이라며 서버가 안 뜬다**
이전 서버가 아직 살아 있는 경우다. `pgrep -fl "run.py web"` 으로 확인하고
`kill $(lsof -ti:8000)` 로 끄거나, `--port 9000` 으로 다른 포트에 띄운다.

**규칙을 고쳤는데 결과가 그대로다**
`classify-rule` 은 기본적으로 아직 분류되지 않은 것만 처리한다.
`--force` 를 붙이거나 웹의 **전체 재분류** 버튼을 쓴다. 한국어 판정도 마찬가지다.

**한국어 사이트가 너무 많이 잡힌다**
`Accept-Language: ko-KR` 때문에 글로벌 사이트가 포함된 것이다. 웹 UI 판정 기준에서
임계값을 120~160으로 올리면 `lang_ko` 하나만으로는 통과하지 못한다.
바꾼 뒤 **전량 재판정**.

**특정 사이트만 결과를 고치고 싶다**
사이트 검수 화면에서 **수정**. 수동 라벨은 재분류해도 유지된다.

**DB를 처음부터 다시 만들고 싶다**
`data/sites.db` 를 지우고 `run.py init` 을 다시 실행한다.
크롤링 결과까지 전부 사라지므로, 판정 결과만 초기화하려면 이렇게 한다.

```bash
./venv/bin/python -c "
from src import db
c = db.connect()
c.execute('DELETE FROM korean'); c.execute('DELETE FROM classification'); c.commit()
print('판정·분류 결과 초기화 (크롤링 데이터는 유지)')
"
```

---

## 9. 명령어 레퍼런스

| 명령 | 설명 | 웹 UI 대응 | 네트워크 | 소요 |
|---|---|---|:---:|---|
| `init` | DB 생성 + CSV 적재 + 규칙 시드 | — | | 1분 |
| `fetch --limit N` | 크롤링 | 크롤링 시작 | ● | 건수에 비례 |
| `retry` | 일시 실패분 재시도 | 실패분 재시도 | ● | 실패 건수에 비례 |
| `detect-korean [--force]` | 한국어 판정 | 한국어 판정 / 전량 재계산 | | 수 분 |
| `compact` | 비한국어 본문 정리 + VACUUM | 본문 정리 + 압축 | | 수 분 |
| `classify-rule [--force]` | 규칙 분류 | 규칙 분류 / 전량 재분류 | | 수 분 |
| `llm-auto [--rounds N]` | 배치 생성→판정→반영 자동 반복 | 자동 판정 실행 | ● | 배치당 수십 초 |
| `llm-export [--size N]` | 세션 판정 배치 생성 | 배치 내보내기 | | 즉시 |
| `llm-import <경로>` | 배치 결과 반영 | 배치 목록의 반영 | | 즉시 |
| `llm-status` | 배치 진행 상황 | 진행 상황 화면 | | 즉시 |
| `export` | 최종 산출물 생성 | 전체 산출물 생성 | | 수십 초 |
| `export --reviewed-only` | 검수완료분만 생성 | 검수완료분만 생성 | | 수십 초 |
| `status` | 전체 진행 상황 요약 | 진행 상황 화면 | | 즉시 |
| `web [--port N]` | 관리 UI 실행 | — | | — |

`init` 만 웹에서 실행할 수 없다. DB가 있어야 웹이 뜨기 때문이다.
