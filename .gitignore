# 가상환경 (venv 안에 절대경로가 박혀 있어 다른 머신에서 못 쓴다)
venv/

# 데이터베이스 — 245MB 본체 + 백업. 재수집으로 복원 가능하고 검수 결과는 별도 백업 관리
data/
*.db
*.db-shm
*.db-wal

# 원본 시드 데이터 (tranco 100만 도메인, 재다운로드 가능)
top-1m.csv
*.csv.zip

# LLM 배치 요청·결과 (파이프라인 실행 산출물, 코드에서 재생성)
llm/

# 내보내기 산출물 (run.py export 로 재생성)
out/

# 로그
web.log
*.log

# 작업 기록 (세션별 로컬 메모)
WORK_LOG.md

# 파이썬
__pycache__/
*.py[cod]
.Python
*.egg-info/

# macOS
.DS_Store
